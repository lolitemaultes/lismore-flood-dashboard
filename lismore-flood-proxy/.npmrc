# Force npm to install packages in project's node_modules
# This prevents cPanel from installing to virtual environment
prefix=${HOME}/.npm-global
save-exact=false
package-lock=true

# Install dependencies locally in this project
global=false
